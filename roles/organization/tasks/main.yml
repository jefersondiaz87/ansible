---
# ===============================
# CREAR / ELIMINAR ORGANIZACIÓN (REST)
# ===============================

- name: Crear organización en VMware Cloud Director vía REST
  ansible.builtin.uri:
    url: "{{ vcd.host }}/cloudapi/1.0.0/orgs"
    method: POST
    headers:
      x-vcloud-authorization: "{{ vcd.access_token }}"
      Accept: "application/json"
      Content-Type: "application/json"
    body_format: json
    body:
      name: "{{ vcd.orgFullName }}"
      displayName: "{{ vcd.orgFullName }}"
      isEnabled: true
    status_code: [201, 200]
    validate_certs: "{{ vcd.verify_ssl_certs | default(false) }}"
  register: org_create

- debug:
    msg: "✅ Organización creada: {{ org_create.json.name }}"

# =======================================
# AGREGAR METADATA A LA ORGANIZACIÓN
# =======================================

- name: Agregar metadata TrialMode si trial=true
  when: trial | bool
  ansible.builtin.uri:
    url: "{{ vcd.hostname }}/api/org/{{ vcd.orgFullName }}/metadata"
    method: POST
    headers:
      x-vcloud-authorization: "{{ vcd.access_token }}"
      Accept: "application/*+json;version={{ vcd.api_version }}"
      Content-Type: "application/vnd.vmware.vcloud.metadata+xml"
    body: |
      <Metadata xmlns="http://www.vmware.com/vcloud/v1.5" type="application/vnd.vmware.vcloud.metadata+xml">
        <MetadataEntry>
          <Key>TrialMode</Key>
          <Value>true</Value>
          <Type>STRING</Type>
        </MetadataEntry>
      </Metadata>
    status_code: [200, 201]
    validate_certs: "{{ vcd.verify_ssl | default(false) }}"
  register: metadata_set

- debug:
    msg: "🏷️ Metadata TrialMode agregada a {{ vcd.orgFullName }}"
  when: trial | bool

# =======================================
# (Opcional) ELIMINAR ORGANIZACIÓN
# =======================================
- name: Eliminar organización si cleanup=true
  when: cleanup | default(false) | bool
  ansible.builtin.uri:
    url: "{{ vcd.hostname }}/cloudapi/1.0.0/orgs/{{ vcd.orgFullName }}"
    method: DELETE
    headers:
      x-vcloud-authorization: "{{ vcd.access_token }}"
      Accept: "application/json"
    status_code: [202, 204, 404]
    validate_certs: "{{ vcd.verify_ssl | default(false) }}"
  register: org_delete

- debug:
    msg: "🗑️ Organización {{ vcd.orgFullName }} eliminada (cleanup activado)"
  when: cleanup | default(false) | bool
