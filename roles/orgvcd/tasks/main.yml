---
# ===========================================================
# VALIDAR SI EL ORG VDC YA EXISTE
# ===========================================================
- name: Verificar si el Org VDC ya existe en VMware Cloud Director
  uri:
    url: "{{ vcd.host }}/api/query?type=orgVdc&filter=name=={{ vcd.orgVdcName }}"
    method: GET
    headers:
      Accept: "application/*+json;version={{ vcd.api_version }}"
      Authorization: "Bearer {{ vcd.access_token }}"
    return_content: yes
    validate_certs: false
  register: vdc_check
  delegate_to: localhost
  failed_when: vdc_check.status not in [200, 201]

- name: Determinar si el Org VDC ya existe
  set_fact:
    vdc_exists: "{{ (vdc_check.json.record is defined) and (vdc_check.json.record | length > 0) }}"

- name: Mostrar estado del VDC
  debug:
    msg: "💡 Org VDC '{{ vcd.orgVdcName }}' {{ 'ya existe' if vdc_exists else 'no existe, se procederá a crear' }}."

# ===========================================================
# CREAR EL ORG VDC (SI NO EXISTE)
# ===========================================================
- name: Crear Org VDC en VMware Cloud Director vía REST
  when: not vdc_exists
  uri:
    url: "{{ vcd.host }}/api/orgVdc"
    method: POST
    headers:
      Accept: "application/*+json;version={{ vcd.api_version }}"
      Authorization: "Bearer {{ vcd.access_token }}"
      Content-Type: "application/json"
    body_format: json
    body:
      name: "{{ vcd.orgVdcName }}"
      org: "{{ vcd.orgFullName }}"
      providerVdcName: "{{ providerVdc | default('ProviderVDC-Default') }}"
      allocationModel: "ReservationPool"
      computeCapacity:
        cpu:
          allocated: "{{ vcd.cpuQuota | int }}"
          limit: "{{ vcd.cpuQuota | int }}"
        memory:
          allocated: "{{ vcd.memoryQuota | int }}"
          limit: "{{ vcd.memoryQuota | int }}"
      storageProfiles:
        - name: "{{ storageProfile | default('Standard') }}"
          limit: "{{ vcd.storageQuota | int }}"
      isEnabled: true
      networkPoolName: "{{ networkPool | default('Default-NetworkPool') }}"
      usesFastProvisioning: true
    validate_certs: false
  register: vdc_create
  delegate_to: localhost
  failed_when: vdc_create.status not in [200, 201, 202]

- name: Confirmar creación del Org VDC
  when: not vdc_exists
  debug:
    msg: "✅ Org VDC '{{ vcd.orgVdcName }}' creado exitosamente."

# ===========================================================
# AGREGAR / ACTUALIZAR METADATA (TRIALMODE)
# ===========================================================
- name: Asignar metadata TrialMode al Org VDC
  uri:
    url: "{{ vcd.host }}/api/vdc/{{ vcd.orgVdcName }}/metadata"
    method: PUT
    headers:
      Accept: "application/*+json;version={{ vcd.api_version }}"
      Authorization: "Bearer {{ vcd.access_token }}"
      Content-Type: "application/json"
    body_format: json
    body:
      metadata:
        - key: "TrialMode"
          value: "{{ 'true' if trial else 'false' }}"
          type: "STRING"
    validate_certs: false
  register: metadata_update
  delegate_to: localhost
  failed_when: metadata_update.status not in [200, 201, 202]

- name: Confirmar metadata aplicada
  debug:
    msg: "🟢 Metadata 'TrialMode={{ 'true' if trial else 'false' }}' aplicada correctamente al Org VDC '{{ vcd.orgVdcName }}'."
